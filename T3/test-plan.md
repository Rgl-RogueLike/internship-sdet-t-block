# Задача T3. Составление тест-плана

## 1. Идентификатор плана тестирования

**TP-003**
План тестирования REST API `GET /products/{productId}/cost`.

---

## 2. Введение

Целью данного документа является описание подхода к тестированию REST API `GET /products/{productId}/cost`, предназначенного для расчета стоимости продукта в зависимости от количества экземпляров и начального индекса с нарастающим итогом.
Документ содержит анализ требований, стратегию тестирования, применяемые техники тест-дизайна и тест-кейсы.

---

## 3. Объект тестирования

Объектом тестирования является REST API 

`GET /products/{productId}/cost`

API предназначено для получения стоимости продукта.

Входные параметры:

- `productId`;
- `authToken`;
- `count`;
- `start`.

## 4. Анализ требований

### Входные параметры

| Параметр | Тип | Обязательный | Допустимые значения |
|-----------|-----|--------------|---------------------|
| `productId` | Path | Да | Идентификатор продукта |
| `authToken` | Query | Да | Строка из 16 буквенно-цифровых символов |
| `count` | Query | Да | 1,2,3,4,5 (и более, с учетом партионности) |
| `start` | Query | Да | 0,1,2,3,4 (и более, с учетом партионности) |

### Бизнес-логика расчета стоимости

| Количество приобретенных продуктов | 1 | 2 | 3 | 4 | 5 |
|-------------------------------------|---|---|---|---|---|
| Стоимость | 30 | 25 | 20 | 15 | 10 |

- Вычисление выполняется нарастающим итогом.
- Приобретение возможно только партиями по 5 экземпляров. При запросе `count > 5` расчет идет по кругу таблицы (6-й экземпляр стоит как 1-й, то есть 30, и т.д.). 

### Возможные ответы сервиса

| HTTP-код | Описание |
|-----------|----------|
| 200 | Стоимость продукта успешно рассчитана |
| 401 | Невалидный или отсутствующий `authToken` |
| 404 | Продукт не найден |
| 500 | Внутренняя ошибка сервера, в том числе при невалидных параметрах `count` и `start`, так как иные коды не предусмотрены | 

## 5. Стратегия тестирования

### Общий подход

Тестирование выполняется методом черного ящика на основе требований к REST API без анализа внутренней реализации сервиса.

Основное внимание уделяется:

- проверке корректной обработки входных параметров;
- проверке математической логики расчета стоимости (нарастающий итог и партионность);
- проверке обработки позитивных и негативных сценариев;
- проверке структуры и содержимого ответов сервиса;
- проверке обработки ошибок.

### Уровни тестирования

В рамках данного тест-плана выполняется функциональное тестирование REST API на уровне системного тестирования.

## 6. Критерии начала тестирования

Тестирование может быть начато при выполнении следующих условий:

- требования к API согласованы;
- REST API доступно для тестирования;
- известны значения тестовых параметров (`productId`, `authToken`);
- подготовлено тестовое окружение.

## 7. Критерии завершения тестирования

Тестирование считается завершенным при выполнении следующих условий:

- выполнены все предусмотренные тест-кейсы;
- проверены позитивные и негативные сценарии;
- выявленные дефекты зарегистрированы;
- результаты тестирования оформлены. 

## 8. Критерии приостановки и возобновления тестирования

### Приостановка тестирования 

Тестирование приостанавливается в следующих случаях:

- сервис недоступен;
- отсутствует тестовое окружение;
- невозможно выполнить тестовые сценарии из-за критического дефекта.

### Возобновление тестирования

Тестирование продолжается после устранения причин приостановки и восстановления работоспособности тестового окружения.

## 9. Среда тестирования

| Компонент | Значение |
|------------|----------|
| Тип тестирования | REST API |
| Метод | GET |
| Формат обмена | JSON |
| Инструмент тестирования | Postman |
| ОС | Windows 11 |

## 10. Результаты тестирования 

Результаты выполнения тест-кейсов фиксируются в тестовой документации.

При обнаружении несоответствий создаются Bug Report с описанием дефекта, шагами воспроизведения, ожидаемым и фактическим результатом.

## 11. Роли и ответственность

| Роль | Ответственность |
|-------|-----------------|
| QA Engineer | Написание плана тестирования, разработка тест-кейсов, выполнение тестирования, составление баг-репортов. |
| QA Lead | Ревью тестовой документации, консультирование, оценка качества тестирования. |
| Разработчик (Backend) | Устранение заведенных дефектов (баг-репортов). |

## 12. Расписание

| Этап | Сроки |
|-------|-------|
| Анализ требований и написание тест-плана | 1 день |
| Ревью тест-плана | 1 день |
| Выполнение тестирования | 1-2 дня |
| Заведение баг-репортов и подготовка отчета о тестировании | 1 день |

## 13. Риски и пути их снижения

| Риск | Вероятность | Влияние | Пути снижения |
|-------|-------------|---------|---------------|
| Неполнота или противоречивость требований | Средняя | Среднее | Вопросы аналитикам/ментору. Фиксация допущений в тест-кейсах |
| Зависимость от тестовых данных | Низкая | Высокое | Предварительная подготовка и фиксация тестовых данных. Возможность самостоятельного создания продукта через другие API |
| Недоступность тестового стенда | Низкая | Высокое | Уведомление команды. Приостановка тестирования до восстановления. Перенос сроков |
| Уход QA-инженера на больничный | Низкая | Среднее | Поддержка актуальности тестовой документации. Любой другой QA в команде сможет продолжить тестирование по написанным тест-кейсам |

## 14. План тестирования

В рамках тестирования необходимо проверить следующие аспекты работы REST API:

1. **Проверка авторизации**
    - запрос с валидным `authToken`;
    - запрос с отсутствующим `authToken`;
    - запрос с невалидным `authToken`.
2. **Проверка параметра `productId`**
    - расчет стоимости существующего продукта;
    - запрос несуществующего продукта.
3. **Проверка бизнес-логики расчета стоимости (параметры `count` и `start`)**
    - расчет полной первой партии (`count = 5`, `start = 0`);
    - расчет неполной партии с начала (`count = 2`, `start = 0`);
    - расчет со смещением `start` (`count = 2`, `start = 2`);
    - расчет 1 товара при максимальном `start` по таблице (`count = 1`, `start = 4`);
    - проверка перехода на новую партию при `count > 5` (`count = 6`, `start = 0`);
    - проверка партионности со смещением `start` (`count = 6`, `start = 4`).
4. **Проверка невалидных значений `count` и `start`**
    - передача `count = 0`;
    - передача отрицательных значений `count` и `start`;  
    - передача строковых значений в числовые параметры.
5. **Проверка успешного ответа**
    - HTTP-код `200`;
    - соответствие структуры JSON требованиям;
    - корректность математического значения `cost`.
6. **Проверка обработки ошибок**
    - возврат HTTP-кода `401` при отсутствии или неверном токене;
    - возврат HTTP-кода `404` при отсутствии продукта;
    - возврат HTTP-кода `500` при ошибке обработки запроса с телом в формате JSON, содержащим поле `errorMessage`.

## 15. Применяемые техники тест-дизайна

### 15.1 Классы эквивалентности

Для сокращения количества тестов входные данные были разделены на классы эквивалентности. Из каждого выбрано по одному или нескольким представителям.

| Параметр | Валидные классы | Невалидные классы | Тест-кейсы |
|-----------|-----------------|-------------------|------------|
| `authToken` | Строка из 16 буквенно-цифровых символов | отсутствует; длина != 16; невалидные символы | TC-001–TC-005, TC-018 |
| `productId` | Существующий идентификатор | Несуществующий идентификатор | TC-001, TC-006, TC-017 |
| `count` | 1-5; > 5 (с учетом партий) | 0; отрицательные; строки | TC-007–TC-016, TC-011–TC-014 |
| `start` | 0-4; > 4 (с учетом партий) | отрицательные; строки | TC-007–TC-016, TC-013 |

### 15.2 Анализ граничных значений

Для параметров `count`, `start` и `authToken` применен анализ граничных значений.

| Параметр | Значение | Ожидаемый результат | Тест-кейс |
|-----------|----------|---------------------|-----------|
| `authToken` | 15 символов | Ошибка авторизации (`401`) | TC-004 |
| `authToken` | 16 символов | Успешная авторизация | TC-001 |
| `authToken` | 17 символов | Ошибка авторизации (`401`) | TC-005 |
| `count` | 0 | Ошибка (`500`) | TC-011 |
| `count` | 1 (минимальное значение) | Успешный расчет (`200`) | TC-001 |
| `count` | 6 (переходное значение) | Успешный расчет (`200`) | TC-010 |
| `start` | 0 (минимальное значение) | Успешный расчет (`200`) | TC-001 |
| `start` | 4 (максимальное по таблице) | Успешный расчет (`200`) | TC-008 |

### 15.3 Негативное тестирование

| Проверяемая ситуация | Тест-кейс |
|-----------------------|-----------|
| Отсутствует `authToken` | TC-002 |
| Невалидный `authToken` | TC-003 |
| Несуществующий `productId` | TC-006 |
| Невалидное значение `count = 0` | TC-011 |
| Отрицательное значение `count` | TC-012 |
| Отрицательное значение `start` | TC-013 |
| Строковое значение `count` | TC-014 |

### 15.4 Попарное тестирование

Для проверки совместной работы параметров `count` и `start` (включая выход за пределы базовой таблицы) использовано попарное тестирование.

Проверяются комбинации параметров:

| `productId` | `authToken` | `count` | `start` | Тест-кейс |
|--------------|-------------|---------|---------|-----------|
| Valid | Valid | 1 | 0 | TC-001 | 
| Invalid | Invalid | 1 | 4 | TC-019 | 
| Valid | Invalid | 1 | 5 | TC-020 | 
| Invalid | Valid | 5 | 0 | TC-021 | 
| Valid | Valid | 5 | 4 | TC-015 |
| Invalid | Invalid | 5 | 5 | TC-022 | 
| Valid | Invalid | 6 | 0 | TC-018 |
| Invalid | Valid | 6 | 4 | TC-023 | 
| Valid | Valid | 6 | 5 | TC-016 |

## 16. Тест-кейсы

### 16.1 Авторизация 

#### TC-001. Получение стоимости продукта с валидным `authToken`

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить получение стоимости продукта с валидным `authToken`. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | Валидный `authToken` (16 символов). `count = 1`. `start = 0`. |
| **Шаги** | 1. Отправить GET-запрос `products/{productId}/cost?authToken=valid&count=1&start=0`. |
| **Ожидаемый результат** | Получен HTTP-код `200`. Тело ответа соответствует требованиям и содержит поле `cost` со значением `30`. |  

#### TC-002. Получение продукта без `authToken`

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку запроса без обязательного параметра `authToken`. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | `authToken` отсутствует. `count = 1`. `start = 0`. |
| **Шаги** | 1. Отправить GET-запрос `products/{productId}/cost?count=1&start=0`. |
| **Ожидаемый результат** | Получен HTTP-код `401`. В ответе отсутствует информация о стоимости. | 

#### TC-003. Получение стоимости с невалидным `authToken` 

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку запроса с `authToken`, не соответствующим требованиям формата. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | `authToken=invalid_token`. `count = 1`. `start = 0`. |
| **Шаги** | 1. Отправить GET-запрос `products/{productId}/cost?authToken=invalid_token&count=1&start=0`. |
| **Ожидаемый результат** | Получен HTTP-код `401`. |  

#### TC-004. Получение стоимости продукта с `authToken` длиной 15 символов

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку запроса с `authToken`, длина которого меньше допустимого. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | `authToken` состоящий из 15 символов. `count = 1`. `start = 0`. |
| **Шаги** | 1. Отправить GET-запрос `products/{productId}/cost?authToken=token&count=1&start=0`, где длина `token` = 15. |
| **Ожидаемый результат** | Получен HTTP-код `401`. |  

#### TC-005. Получение стоимости продукта с `authToken` длиной 17 символов

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку запроса с `authToken`, длина которого больше допустимого. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | `authToken` состоящий из 17 символов. `count = 1`. `start = 0`. |
| **Шаги** | 1. Отправить GET-запрос `products/{productId}/cost?authToken=token&count=1&start=0`, где длина `token` = 17. |
| **Ожидаемый результат** | Получен HTTP-код `401`. | 

### 16.2 Проверка productId

#### TC-006. Получение стоимости несуществующего продукта

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку запроса с `productId`, которого нет в системе. |
| **Предусловия** | Используется продукт, отсутствующий в системе. |
| **Тестовые данные** | Валидный `authToken`. `count = 1`. `start = 0`. `productId`, отсутствующий в системе |
| **Шаги** | 1. Отправить GET-запрос с валидными `authToken`, `count = 1`, `start = 0` и несуществующим `productId`. |
| **Ожидаемый результат** | Получен HTTP-код `404`. |  

### 16.3 Проверка бизнес-логики расчета стоимости (`count` и `start`)

#### TC-007. Расчет полной первой партии (`count = 5`, `start = 0`)

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить расчет стоимости за 5 экземпляров с нулевого индекса. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | Валидный `authToken`. `count = 5`. `start = 0`. |
| **Шаги** | 1. Отправить GET-запрос с валидными `authToken`, `count = 5`, `start = 0`. |
| **Ожидаемый результат** | Получен HTTP-код `200`. Тело ответа: `{"cost": 100}` (30 + 25 + 20 + 15 + 10). |  

#### TC-008. Расчет 1 товара при максимальном `start` (`count = 1`, `start = 4`)

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить расчет стоимости 1 товара, начиная с 5-го по таблице. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | Валидный `authToken`. `count = 1`. `start = 4`. |
| **Шаги** | 1. Отправить GET-запрос с валидными `authToken`, `count = 1`, `start = 4`.  |
| **Ожидаемый результат** | Получен HTTP-код `200`. Тело ответа `{"cost": 10}`. |  

#### TC-009. Расчет со смещением `start` внутри партии (`count = 2`, `start = 2`)

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить расчет стоимости за 2 экземпляра, начиная с 3-го индекса. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | Валидный `authToken`. `count = 2`. `start = 2`. |
| **Шаги** | 1. Отправить GET-запрос с валидными `authToken`, `count = 2`, `start = 2`. |
| **Ожидаемый результат** | Получен HTTP-код `200`. Тело ответа `{"cost": 35}`(20 + 15). |  

#### TC-010. Проверка партионности при выходе за пределы 5 (`count = 6`, `start = 0`)

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить расчет стоимости при запросе более 5 экземпляров (переход на новую партию). |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | Валидный `authToken`. `count = 6`. `start = 0`. |
| **Шаги** | 1. Отправить GET-запрос с валидными `authToken`, `count = 6`, `start = 0`. |
| **Ожидаемый результат** | Получен HTTP-код `200`. Тело ответа `{"cost": 130}`(100 за первые 5 + 30 за 6-й). |  

### 16.4 Проверка невалидных значений count и start

#### TC-011. Запрос с параметром `count = 0`

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку запроса с `count = 0`. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | Валидный `authToken`. `count = 0`. `start = 0`. |
| **Шаги** | 1. Отправить GET-запрос с валидными `authToken`, `count = 0`, `start = 0`. |
| **Ожидаемый результат** | Получен HTTP-код `500`. Тело ответа содержит поле `errorMessage`. |  

#### TC-012. Запрос с отрицательным значением `count = -1`

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку запроса с отрицательным `count`. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | Валидный `authToken`. `count = -1`. `start = 0`. |
| **Шаги** | 1. Отправить GET-запрос с валидными `authToken`, `count = -1`, `start = 0`. |
| **Ожидаемый результат** | Получен HTTP-код `500`. Тело ответа содержит поле `errorMessage`. |  

#### TC-013. Запрос с отрицательным значением `start = -1`

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку запроса с отрицательным `start`. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | Валидный `authToken`. `count = 1`. `start = -1`. |
| **Шаги** | 1. Отправить GET-запрос с валидными `authToken`, `count = 1`, `start = -1`. |
| **Ожидаемый результат** | Получен HTTP-код `500`. Тело ответа содержит поле `errorMessage`. |  

#### TC-014. Запрос со строковым значением `count = abc`

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку запроса с нечисловым значением `count`. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | Валидный `authToken`. `count = abc`. `start = 0`. |
| **Шаги** | 1. Отправить GET-запрос с валидными `authToken`, `count = abc`, `start = 0`. |
| **Ожидаемый результат** | Получен HTTP-код `500`. Тело ответа содержит поле `errorMessage`. |  

### 16.5 Проверка комбинаций параметров

#### TC-015. Комбинация `count = 5`, `start = 4` (Valid, Valid)

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить расчет при запросе 5 товаров, начиная с 5-го (переход партии). |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | Валидный `authToken`. `count = 5`. `start = 4`. |
| **Шаги** | 1. Отправить GET-запрос с валидными `authToken`, `count = 5`, `start = 4`. |
| **Ожидаемый результат** | Получен HTTP-код `200`. Стоимость: 10 (за 5-й) + 30+25+20+15 (за с 6-го по 9-й) = `{"cost": 100}`. |  

#### TC-016. Комбинация `count = 6`, `start = 5` (Valid, Valid)

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить расчет при выходе обоих параметров за базовую таблицу. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | Валидный `authToken`. `count = 6`. `start = 5`. |
| **Шаги** | 1. Отправить GET-запрос с валидными `authToken`, `count = 6`, `start = 5`. |
| **Ожидаемый результат** | Получен HTTP-код `200`. Стоимость: 30+25+20+15+10 (за 6-10) + 30 (за 11-й) = `{"cost": 130}`. |  

#### TC-017. Комбинация `productId = Invalid`, `count = 1`, `start = 0` (Invalid, Valid, Valid)

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку при невалидном `productId` и валидных `count` и `start`. |
| **Предусловия** | В системе отсутствует указанный `productId`. |
| **Тестовые данные** | Валидный `authToken`. Несуществующий `productId`. `count = 1`. `start = 0`. |
| **Шаги** | 1. Отправить GET-запрос с валидными `authToken`, несуществующим `productId`, `count = 1`, `start = 0`. |
| **Ожидаемый результат** | Получен HTTP-код `404`. | 

#### TC-018. Комбинация `authToken = Invalid`, `count = 6`, `start = 0` (Invalid, Valid, Valid)

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку при невалидном `authToken` и валидных `count` и `start`. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | Невалидный `authToken`. `count = 6`. `start = 0`. |
| **Шаги** | 1. Отправить GET-запрос с невалидным `authToken`, `count = 6`, `start = 0`. |
| **Ожидаемый результат** | Получен HTTP-код `401`. |  

#### TC-019. Комбинация `productId = Invalid`, `authToken = Invalid`, `count = 1`, `start = 4` (Invalid, Invalid)

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку при невалидном `productId` и невалидном `authToken`. |
| **Предусловия** | В системе отсутствует указанный `productId`. |
| **Тестовые данные** | Несуществующий `productId`. Невалидный `authToken`. `count = 1`. `start = 4`. |
| **Шаги** | 1. Отправить GET-запрос с невалидным `authToken` и невалидным `productId`, `count = 1`, `start = 4`. |
| **Ожидаемый результат** | Получен HTTP-код `401`. |  

#### TC-020. Комбинация `productId = Valid`, `authToken = Invalid`, `count = 1`, `start = 5` (Valid, Invalid)

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку при валидном `productId`, невалидном `authToken` и `start` вне базовой таблицы. |
| **Предусловия** | В системе существует продукт с известным `productId`. |
| **Тестовые данные** | Валидный `productId`. Невалидный `authToken`. `count = 1`. `start = 5`. |
| **Шаги** | 1. Отправить GET-запрос с невалидным `authToken` и валидным `productId`, `count = 1`, `start = 5`. |
| **Ожидаемый результат** | Получен HTTP-код `401`. |  

#### TC-021. Комбинация `productId = Invalid`, `authToken = Valid`, `count = 5`, `start = 0` (Invalid, Valid)

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку при невалидном `productId` и валидных `count` и `start`. |
| **Предусловия** | В системе отсутствует указанный `productId`. |
| **Тестовые данные** | Несуществующий `productId`. Валидный `authToken`. `count = 5`. `start = 0`. |
| **Шаги** | 1. Отправить GET-запрос с валидным `authToken` и несуществующим `productId`, `count = 5`, `start = 0`. |
| **Ожидаемый результат** | Получен HTTP-код `404`. |  

#### TC-022. Комбинация `productId = Invalid`, `authToken = Invalid`, `count = 5`, `start = 5` (Invalid, Invalid)

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку при невалидном `productId` и невалидном `authToken` и `start` вне базовой таблицы. |
| **Предусловия** | В системе отсутствует продукт с указанным `productId`. |
| **Тестовые данные** | Несуществующий `productId`. Невалидный `authToken`. `count = 5`. `start = 5`. |
| **Шаги** | 1. Отправить GET-запрос с невалидным `authToken` и несуществующим `productId`, `count = 5`, `start = 5`. |
| **Ожидаемый результат** | Получен HTTP-код `401`. |  

#### TC-023. Комбинация `productId = Invalid`, `authToken = Valid`, `count = 6`, `start = 4` (Invalid, Valid)

| Поле | Значение |
|-------|----------|
| **Цель** | Проверить обработку при невалидном `productId` и `count` вне базовой таблицы. |
| **Предусловия** | В системе отсутствует указанный `productId`. |
| **Тестовые данные** | Несуществующий `productId`. Валидный `authToken`. `count = 6`. `start = 4`. |
| **Шаги** | 1. Отправить GET-запрос с валидным `authToken` и несуществующим `productId`, `count = 6`, `start = 4`. |
| **Ожидаемый результат** | Получен HTTP-код `404`. |  

### 16.6 Проверка успешного ответа

Покрывается тест-кейсами TC-001, TC-007, TC-009 и др.

- HTTP-код `200`;
- соответствие структуры JSON требованиям `{"cost": <число>}`.

### 16.7 Проверка обработки ошибок

Раздел покрывается тест-кейсами:

- TC-002, TC-003, TC-004, TC-005, TC-018, TC-019, TC-020, TC-022 - проверка HTTP-кода `401`;
- TC-006, TC-017, TC-021, TC-023 – проверка HTTP-кода `404`;
- TC-011, TC-012, TC-013, TC-014 – проверка HTTP-кода `500` и структуры сообщения об ошибке `{"errorMessage": "..."}`.
